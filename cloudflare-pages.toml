# Cloudflare Pages configuration - ensure correct Node version and build outputs
[build]
command = "npm run build"
directory = "dist"
node_version = "20.19.0"
install_command = "npm ci"

[env.production]
# Uncomment and set any env vars needed for build or runtime
# MY_ENV_VAR = "value"
